import {LoginScreen } from '../routes/WithLoadingScreens';
import { LoggedScreen } from '../containers/LoggedScreen';

export const authScreens = {
    'Zaloguj sie': LoginScreen
}

export const userScreens = {
    'Panel Użytkownika': LoggedScreen,
}